<UserControl x:Class="WunderTask.Component.MyTaskList"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             Name="Root"
             DataContext="{Binding ElementName=Root, Path=.}"
             d:DesignHeight="300" d:DesignWidth="300">
    <StackPanel Orientation="Horizontal" Height="30" Margin="10,0,0,0" >
        <Image Source="..\Images\align_just.png" Width="20" VerticalAlignment="Center"></Image>
        <TextBlock Margin="12,0,0,0" Text="{Binding Path=Text}" FontSize="14" VerticalAlignment="Center" />
    </StackPanel>
</UserControl>
